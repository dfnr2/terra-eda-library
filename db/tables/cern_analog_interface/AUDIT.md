# cern_analog_interface — Human Audit

Source: CERN `Analog & Interface` (2199 parts). Generated by `run_100_cern_import.py`.

## Automated (must pass before audit)
- [ ] `CERN_SQLITE=/users/dave/vsrc/cern-kicad-libs/CERN.sqlite make cern_analog_interface-test` green
      (row count 2199, no dup unique_ids, known-part NE555D, nicks registered, all
      footprints AND symbols resolve)
- [ ] `CERN_SQLITE=/users/dave/vsrc/cern-kicad-libs/CERN.sqlite uv run pytest -q` green

## Sampling (audit ≥ 20 parts)
- [ ] MPN matches the datasheet exactly
- [ ] `package` / `pin_count` match
- [ ] `kicad_symbol` and `kicad_footprint` render in KiCad

## No type tail (by design)
- [ ] This is a grab-bag category (level translators, RS-485, timers, isolators, current
      sensors, drivers, …) with device-named symbols and no single type dimension, so the
      schema carries only the core + pin_count/component_height. Confirm acceptable.

## 3D model coverage
- [ ] 1610/2199 (73%) via the new dimensioned QFN/DFN/QFP resolver plus standard SO/DIP
      packages. Residual: vendor SON (no parametric KiCad model), exotic (AF320, LGA73),
      blank-package, and a few SO/TSSOP body variants → human drop-folder.

## CERN data fixups
- [ ] `cern_libmap.ITEM_FIXUP` corrects: footprint `INFININEON…`→`INFINEON…`,
      `QFP80P900X900X160-32AN`→`…-32N`; and symbol `TXS0108ERGY`→`TXS0108E_a` (QFN variant
      symbol was never created; same logical part). Confirm the substitutions.

## Known carve-outs
- [ ] Intra-CERN MPN-collision variants kept distinct via `Part Number Nocolon`.
- [ ] No terra reconciliation (new category).

## Sign-off
- Auditor: ______  Date: ______
