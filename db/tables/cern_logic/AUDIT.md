# cern_logic — Human Audit

Source: CERN `Logic` (761 parts). Generated by `run_100_cern_import.py`.

## Automated (must pass before audit)
- [ ] `make cern_logic-test` green (row count 761, no dup unique_ids, known-part
      MCP4728-E/UN, nicks registered, all footprints AND symbols resolve)
- [ ] `uv run pytest -q` green

## Sampling (audit ≥ 20 parts)
- [ ] MPN matches the datasheet exactly
- [ ] `package` / `pin_count` match
- [ ] `kicad_symbol` and `kicad_footprint` render in KiCad

## No type tail (by design)
- [ ] "Logic" is a grab-bag of complex digital ICs (FPGAs, DSPs, flash/memory, DACs,
      serdes, …) with device-named symbols and no single type dimension, so the schema
      carries only the core + pin_count/component_height. Confirm acceptable.

## 3D model coverage
- [ ] 494/761 (64%) — BGA/QFP-heavy. The new dimensioned BGA resolver
      (`model_map._resolve_bga`) matches ball count + pitch + nearest body. Residual is
      genuine: BGA ball-counts/pitches KiCad doesn't ship (BGA484@0.8mm, 676, 96-stacked,
      153, 288, 672, 1517), vendor SON, and memory TSOP → human drop-folder.

## Known carve-outs
- [ ] Intra-CERN MPN-collision variants kept distinct via `Part Number Nocolon`.
- [ ] No terra reconciliation (new category; overlaps conceptually with legacy
      ic_memory/ic_microcontrollers, but those are separate tables).

## Sign-off
- Auditor: ______  Date: ______
