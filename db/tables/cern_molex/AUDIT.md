# cern_molex — Human Audit

Source: CERN `MOLEX` (503 connectors). Generated by `run_100_cern_import.py`.

## Automated (must pass before audit)
- [ ] `make cern_molex-test` green (row count 503, no dup unique_ids, known-part 105133-0011,
      nicks registered, all footprints AND symbols resolve)
- [ ] `uv run pytest -q` green

## Sampling (audit ≥ 20 parts)
- [ ] MPN matches the datasheet exactly
- [ ] `pin_count` matches; series/circuits consistent with the description
- [ ] `kicad_symbol` and `kicad_footprint` render in KiCad

## Structure notes (first connector vendor)
- [ ] Connectors share ONE symbol lib "Connectors" -> `kicad_symbols/cern-connectors.kicad_sym`
      (reused by all connector vendors). Footprints are per-vendor: `MOLEX SMD/THD` ->
      `cern-molex-smd/thd`. No type-specific tail (symbol + pin_count carry the type).

## 3D model coverage
- [ ] 4/503 (0%) for now. CERN footprints are vendor part-numbered (`MOLEX_90130-1206`), not
      IPC-dimensioned, so the dimension resolvers don't apply. KiCad DOES ship many series
      models (`Connector_Molex`: KK-254, PicoBlade, Micro-Fit, Mini-Fit, Milli-Grid, …) and the
      CERN descriptions carry the series + circuit count — a description-series + pin-count
      resolver is feasible (pending). Until then connectors go to the human drop-folder.

## Known carve-outs
- [ ] Intra-CERN MPN-collision variants kept distinct via `Part Number Nocolon`.
- [ ] No terra reconciliation (new category; legacy `connectors` table is separate).

## Sign-off
- Auditor: ______  Date: ______
