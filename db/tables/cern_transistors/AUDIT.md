# cern_transistors — Human Audit

Source: CERN `Transistors` (685 parts). Generated by `run_100_cern_import.py`.

## Automated (must pass before audit)
- [ ] `make cern_transistors-test` green
      (row count 685, no dup unique_ids, known-part mapping TIP41C, transistor_type sanity,
      nicks registered, all footprints resolve to real .kicad_mod files)
- [ ] `uv run pytest -q` green

## Sampling (audit ≥ 20 parts spanning transistor_type values)
For each sampled part, open its verified datasheet and confirm:
- [ ] MPN matches the datasheet exactly
- [ ] `transistor_type` (NPN/PNP/N-MOSFET/JFET/IGBT…) matches the datasheet
- [ ] `package` / `pin_count` match
- [ ] `kicad_symbol` and `kicad_footprint` render in KiCad

## transistor_type derivation
- [ ] `transistor_type` is parsed from the CERN symbol name (e.g. `N-MOSFET-D 1G 2S 3D`
      → `N-MOSFET-D`); 476/685 typed. The 209 blank rows are symbols that are a bare device
      part number (no type prefix). Confirm leaving those blank is acceptable.

## 3D model coverage
- [ ] 499/685 (72%) footprints mapped to KiCad bundled models. Residual is genuine tail:
      horizontal TO-247 (no KiCad model), power modules (SOT-227B, ECONODUAL2, IXYS/FUJI),
      vendor-exotic SMD (PG-TDSON, PowerPAK, DFN/SON). Confirm these go to the human
      drop-folder, not a downloader.

## Known carve-outs
- [ ] Intra-CERN MPN-collision variants kept distinct, disambiguated by `Part Number
      Nocolon` in `unique_id`; `mpn` repeats. Confirm desired (vs. collapsing variants).
- [ ] No terra reconciliation performed (transistors is a new category). Confirm no
      pre-existing terra transistor duplicates a CERN MPN that should have merged.

## Sign-off
- Auditor: ______  Date: ______
