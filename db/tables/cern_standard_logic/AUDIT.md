# cern_standard_logic — Human Audit

Source: CERN `Standard Logic` (803 parts). Generated by `run_100_cern_import.py`.

## Automated (must pass before audit)
- [ ] `make cern_standard_logic-test` green
      (row count 803, no dup unique_ids, known-part SN7406N, nicks registered, all
      footprints AND symbols resolve)
- [ ] `uv run pytest -q` green

## Sampling (audit ≥ 20 parts)
- [ ] MPN matches the datasheet exactly
- [ ] `package` / `pin_count` match
- [ ] `kicad_symbol` (the 74xx function symbol) and `kicad_footprint` render in KiCad

## No type tail (by design)
- [ ] 74xx-series logic with function-named symbols (7400/7404/…); the function is the
      symbol itself and there is no separate type dimension to derive, so the schema carries
      only the core + pin_count/component_height. Confirm acceptable.

## 3D model coverage
- [ ] 751/803 (93%) via the accumulated SO/DIP/TSSOP map + dimensioned QFN/QFP resolver.
      Residual: a few vendor SON/QFN, SOT483, SSOP56, blank-package → human drop-folder.

## CERN data fixups
- [ ] `cern_libmap.ITEM_FIXUP`: footprint `TEXAS_DYY0016A` → `TEXAS_DYY0016A - duplicate`
      (the PcbLib file carries an Altium "- duplicate" export suffix). Confirm.

## Known carve-outs
- [ ] Intra-CERN MPN-collision variants kept distinct via `Part Number Nocolon`.
- [ ] No terra reconciliation (new category; legacy `ic_logic` is separate).

## Sign-off
- Auditor: ______  Date: ______
