# cern_dc_dc_converters — Human Audit

Source: CERN `DC-DC Converters` (444 parts). Generated by `run_100_cern_import.py`.

## Automated (must pass before audit)
- [ ] `make cern_dc_dc_converters-test` green (row count 444, no dup unique_ids, known-part
      TEN 15-2411, nicks registered, all footprints AND symbols resolve)
- [ ] `uv run pytest -q` green

## Sampling (audit ≥ 20 parts)
- [ ] MPN matches the datasheet exactly
- [ ] `pin_count` matches; output count (single/dual) consistent with the symbol
- [ ] `kicad_symbol` and `kicad_footprint` render in KiCad

## No type tail (by design)
- [ ] Vendor power modules (TRACO, MURATA, …) with module-named symbols/footprints and a
      blank `Case`, so no type-specific tail. Confirm acceptable.

## 3D model coverage
- [ ] 2/444 (0%) — **expected**. These are brick/SIP power modules with bespoke vendor
      footprints; KiCad ships no generic 3D models for them, so they all go to the human
      drop-folder. Not a regression.

## Known carve-outs
- [ ] Intra-CERN MPN-collision variants kept distinct via `Part Number Nocolon`.
- [ ] No terra reconciliation (new category).

## Sign-off
- Auditor: ______  Date: ______
