# cern_op_amps — Human Audit

Source: CERN `Operational Amplifiers` (1569 parts). Generated by `run_100_cern_import.py`.

## Automated (must pass before audit)
- [ ] `make cern_op_amps-test` green
      (row count 1569, no dup unique_ids, known-part LM358AD, amplifier_type normalized,
      nicks registered, all footprints resolve)
- [ ] `uv run pytest -q` green

## Sampling (audit ≥ 20 parts spanning amplifier_type / channels)
- [ ] MPN matches the datasheet exactly
- [ ] `amplifier_type` / `channels` match (e.g. LM324 = Operational Amplifier, 4)
- [ ] `package` / `pin_count` match
- [ ] `kicad_symbol` and `kicad_footprint` render in KiCad

## tail derivation (amplifier_type, channels)
- [ ] `amplifier_type` is the base class parsed from the CERN symbol name (cut at
      Type/xN/[alt]/+/EP): Operational Amplifier (898), Differential (68), Monolithic (19),
      Buffer (7); blank (577) = device-named symbols. `channels` from the `xN` token (default
      1). Confirm acceptable.

## 3D model coverage
- [ ] 1361/1569 (86%). Residual is dimensioned QFN/SON packages (QFN16/10/12/32, SON8/12)
      that need a footprint-name dimension resolver (shared future lever for logic/analog),
      plus a few one-offs (DF782, SOIC16W) and blank-package parts → human drop-folder.

## Known carve-outs
- [ ] CERN symbols include literal `[alt]` (De Morgan) variants as distinct symbols; the
      `kicad_symbol` ref preserves them and they resolve. Confirm desired.
- [ ] Intra-CERN MPN-collision variants kept distinct via `Part Number Nocolon`.
- [ ] No terra reconciliation (cern_op_amps is a new category; legacy `ic_opamp` is separate).

## Sign-off
- Auditor: ______  Date: ______
