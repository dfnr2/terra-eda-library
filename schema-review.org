#+TITLE: Phase 0 — Part-Type Schema Review

* RESUME BRIEFING (read first — full context to continue cold)
  :STATUS: Awaiting Dave's review of THIS file. He is editing the Decision cells below.
  When he says "done", parse his marks and execute Phase 0 (plan:
  2026-06-10-phase0-schema-consolidation.md): build db/schema/core.sql (the 34
  shared core cols) + db/schema/types/<type>.sql fragments from the APPROVED columns
  here, write tools/gen_schema.py (generates each table's _0_schema.sql = core +
  fragment), add the equivalence + drift tests, regenerate so cern_<type> and the
  native <type> share one tail. This file's marks ARE the Task 5-12 column lists.

  HOW DAVE MARKS THIS FILE:
  - Universal drop section: DROP-ALL yes/no (drop the 12 legacy symbols cols everywhere).
  - Each type table: edit Decision = keep|add|drop; resolve every 'drop?' row
    (drop / rename / keep). Transistors is the one with real judgment (7 existing
    bjt params being consolidated; vce_max->v_ce_ds_max, ic_max->i_c_d_max renames).
  - connectors: ADOPT yes/no (reuse native 38-col schema for all 17 connector tables).
  - CERN-only table: Confirm yes (bless existing tail). Passives are deferred.

  PROJECT STATE (where the whole effort stands):
  - CERN import COMPLETE: 35 cern_* tables, ~15.4k parts (skill: skills/port-cern-library).
  - Datasheets: ~1,556 matched/equivalent banked via cheap tiers (TI template ~1253,
    commodity Diodes/TI ~290, open-CDN search). Resumable system: skills/fetch-cern-
    datasheets/SKILL.md, tools/datasheets/*, log = assets/datasheets/acquisition.jsonl,
    browse = assets/datasheets/by-name/. ~8.8k remain = EXPENSIVE LONG TAIL, DEFERRED
    (bot-blocked majors like onsemi 403; revisit later w/ Playwright or leave partial).
  - STRATEGY (memory: terra-library-direction): go-forward terra lib is KiCad-symbol-
    derived; source from EASIEST MAJOR manufacturer; datasheets MUST match the part
    (keep original manufacturer, don't switch to a 2nd source); CERN = reuse cache;
    prefer parts we can match. Phase 0 (this) is the bridge to the terra PASSIVE
    generators (the real high-ROI work: resistors/caps/inductors via per-series
    generators, like the existing resistors_smt 22k / capacitors_smt 4.8k).
  - Manufacturer-switch idea was SCRAPPED (all switch candidates were major originals;
    DB never changed). open-CDN majors (Microchip/PowerIntegrations/Nexperia/TI/Diodes/
    Wurth) are curl-friendly; onsemi/ADI/ST bot-block.

  KEY FILES: this review + 2026-06-10-phase0-schema-consolidation.md (the plan) +
  2026-06-10-datasheet-param-resolver-design.md (resolver spec). Tools: gen_schema_
  review.py (this), tools/datasheets/{build_worklist,remaining,ingest,audit,
  link_by_name,template_sweep,propose_switches}.py, tools/fetch_datasheets.workflow.js.
  Branch: feat/tier-tag-system. Specs go at REPO ROOT (not docs/superpowers/), per Dave.

#+NOTE: Decision = keep | drop | add (edit cells). 'In' shows which tables already have the column.
#+NOTE: Proposed canonical tail per type. Core = the 34 shared columns (not shown).

* Universal drop (applies to ALL native non-passive tables) — confirm once
  These legacy flat-`symbols` columns are dropped everywhere (duplicates of core fields or dead SPICE/temp placeholders). Object here if you want any kept globally:
  class, component_type, component_value, composition, number_of_pins, reference, sim_library, sim_type, temp_coeff, temp_operating, temp_soldering, temp_storage
  Decision: DROP-ALL (yes/no)

* diodes   (tables: diodes, cern_diodes)
| Decision | Column | Type | In | Rationale |
|----------+--------+------+----+-----------|
| keep | diode_type | TEXT | cern | Type: rectifier/schottky/zener/tvs/small-signal (CLAUDE.md, CERN) |
| keep | voltage_rating | TEXT | cern | Vr reverse standoff/working (CLAUDE.md, CERN) |
| add | forward_voltage | TEXT | NEW | Vf (CLAUDE.md) |
| add | forward_current | TEXT | NEW | If (CLAUDE.md) |
| keep | current_rating | TEXT | native | Io rectifier average (CLAUDE.md) |
| keep | power_rating | TEXT | cern+native | Pd for TVS/zener (CERN) |
| drop? | tolerance | - | native | existing column not in proposal — drop, rename, or keep? |

* transistors   (tables: bjt, mosfet, cern_transistors)
| Decision | Column | Type | In | Rationale |
|----------+--------+------+----+-----------|
| keep | transistor_type | TEXT | cern | npn/pnp/nmos/pmos/igbt/jfet (CLAUDE.md, CERN) |
| add | channels | INTEGER | NEW | dual/single packages |
| add | v_ce_ds_max | TEXT | NEW | Vce(BJT)/Vds(FET) (CLAUDE.md) |
| add | i_c_d_max | TEXT | NEW | Ic/Id (CLAUDE.md) |
| keep | power_dissipation | TEXT | native | Pd (bjt schema) |
| keep | hfe_typ | TEXT | native | BJT gain; null for FET (bjt schema) |
| add | rds_on | TEXT | NEW | FET on-resistance; null for BJT |
| add | vgs_th | TEXT | NEW | FET threshold; null for BJT |
| keep | transition_freq | TEXT | native | ft (bjt schema) |
| keep | temp_junction_max | TEXT | native | Tj max (bjt schema) |
| drop? | bjt_type | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | hfe_max | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | hfe_min | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | ib_max | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | ic_max | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | power_rating | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | tolerance | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | vcb_max | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | vce_max | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | veb_max | - | native | existing column not in proposal — drop, rename, or keep? |

* op_amps   (tables: ic_opamp, cern_op_amps)
| Decision | Column | Type | In | Rationale |
|----------+--------+------+----+-----------|
| keep | amplifier_type | TEXT | cern | voltage-fb/current-fb/instrumentation (CERN) |
| keep | channels | INTEGER | cern | channel count (CLAUDE.md, CERN) |
| add | gain_bandwidth | TEXT | NEW | GBW (CLAUDE.md) |
| add | slew_rate | TEXT | NEW | slew rate (CLAUDE.md) |
| add | input_offset | TEXT | NEW | input offset (CLAUDE.md) |
| add | input_noise | TEXT | NEW | noise (CLAUDE.md) |
| add | supply_voltage | TEXT | NEW | supply range |
| drop? | power_rating | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | tolerance | - | native | existing column not in proposal — drop, rename, or keep? |

* logic   (tables: ic_logic, cern_logic, cern_standard_logic)
| Decision | Column | Type | In | Rationale |
|----------+--------+------+----+-----------|
| add | logic_family | TEXT | NEW | 74HC/74LVC/4000 (CLAUDE.md) |
| add | gate_function | TEXT | NEW | and/or/nand/mux/ff/buffer (CLAUDE.md) |
| add | channels | INTEGER | NEW | gates/bits per package |
| add | propagation_delay | TEXT | NEW | tpd (CLAUDE.md) |
| add | supply_voltage | TEXT | NEW | voltage (CLAUDE.md) |
| drop? | power_rating | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | tolerance | - | native | existing column not in proposal — drop, rename, or keep? |

* analog   (tables: ic_analog, cern_analog_interface)
| Decision | Column | Type | In | Rationale |
|----------+--------+------+----+-----------|
| add | function_type | TEXT | NEW | adc/dac/comparator/mux/vref/interface (CLAUDE.md) |
| add | channels | INTEGER | NEW | channel count |
| add | resolution_bits | TEXT | NEW | ADC/DAC resolution |
| add | interface | TEXT | NEW | spi/i2c/parallel |
| add | supply_voltage | TEXT | NEW | supply range |
| drop? | power_rating | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | tolerance | - | native | existing column not in proposal — drop, rename, or keep? |

* leds   (tables: leds, cern_leds_displays)
| Decision | Column | Type | In | Rationale |
|----------+--------+------+----+-----------|
| keep | color | TEXT | cern | color (CLAUDE.md, CERN) |
| keep | wavelength_nm | TEXT | cern | wavelength (CLAUDE.md, CERN) |
| keep | forward_voltage_v | TEXT | cern | Vf (CLAUDE.md, CERN) |
| keep | current_max_ma | TEXT | cern | If max (CERN) |
| keep | luminous_intensity | TEXT | cern | intensity (CLAUDE.md, CERN) |
| add | viewing_angle | TEXT | NEW | viewing angle (CLAUDE.md) |
| drop? | current_rating | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | tolerance | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | voltage_rating | - | native | existing column not in proposal — drop, rename, or keep? |

* switches   (tables: switches, cern_switches)
| Decision | Column | Type | In | Rationale |
|----------+--------+------+----+-----------|
| keep | switch_type | TEXT | cern | tactile/toggle/dip/rotary/slide (CLAUDE.md, CERN) |
| add | poles | INTEGER | NEW | poles (CLAUDE.md) |
| add | throws | INTEGER | NEW | throws (CLAUDE.md) |
| keep | current_rating | TEXT | native | contact current |
| add | voltage_rating | TEXT | NEW | contact voltage |
| add | actuation_force | TEXT | NEW | actuation force (CLAUDE.md) |
| drop? | tolerance | - | native | existing column not in proposal — drop, rename, or keep? |

* inductors   (tables: inductors)
| Decision | Column | Type | In | Rationale |
|----------+--------+------+----+-----------|
| add | inductance | TEXT | NEW | inductance (CLAUDE.md) |
| keep | tolerance | TEXT | native | tolerance (CLAUDE.md) |
| keep | current_rating | TEXT | native | rated current (CLAUDE.md) |
| add | saturation_current | TEXT | NEW | Isat (CLAUDE.md) |
| add | dcr | TEXT | NEW | DC resistance |
| add | srf | TEXT | NEW | self-resonant freq |

* ferrites   (tables: ferrites)
| Decision | Column | Type | In | Rationale |
|----------+--------+------+----+-----------|
| add | impedance_at_freq | TEXT | NEW | impedance @ freq (CLAUDE.md) |
| add | dc_resistance | TEXT | NEW | DCR (CLAUDE.md) |
| add | current_rating | TEXT | NEW | rated current (CLAUDE.md) |
| drop? | power_rating | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | tolerance | - | native | existing column not in proposal — drop, rename, or keep? |

* ic_drivers   (tables: ic_drivers)
| Decision | Column | Type | In | Rationale |
|----------+--------+------+----+-----------|
| add | driver_type | TEXT | NEW | gate/motor/led/line driver |
| add | output_current | TEXT | NEW | output current (CLAUDE.md) |
| add | channels | INTEGER | NEW | channels (CLAUDE.md) |
| add | logic_level | TEXT | NEW | logic level (CLAUDE.md) |
| drop? | current_rating | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | power_rating | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | tolerance | - | native | existing column not in proposal — drop, rename, or keep? |

* ic_memory   (tables: ic_memory)
| Decision | Column | Type | In | Rationale |
|----------+--------+------+----+-----------|
| add | memory_type | TEXT | NEW | sram/dram/flash/eeprom (CLAUDE.md) |
| add | capacity | TEXT | NEW | capacity (CLAUDE.md) |
| add | speed | TEXT | NEW | speed (CLAUDE.md) |
| add | interface | TEXT | NEW | spi/i2c/parallel (CLAUDE.md) |
| drop? | power_rating | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | tolerance | - | native | existing column not in proposal — drop, rename, or keep? |

* ic_microcontrollers   (tables: ic_microcontrollers)
| Decision | Column | Type | In | Rationale |
|----------+--------+------+----+-----------|
| add | family | TEXT | NEW | family (CLAUDE.md) |
| add | flash_size | TEXT | NEW | flash (CLAUDE.md) |
| add | ram_size | TEXT | NEW | RAM (CLAUDE.md) |
| add | gpio_count | INTEGER | NEW | GPIO count (CLAUDE.md) |
| add | core | TEXT | NEW | CPU core |
| drop? | power_rating | - | native | existing column not in proposal — drop, rename, or keep? |
| drop? | tolerance | - | native | existing column not in proposal — drop, rename, or keep? |

* connectors   (tables: connectors, cern_molex, cern_samtec)
  Adopt the existing native `connectors` 38-column schema verbatim for all 17 connector tables. Decision: ADOPT (yes/no).
  current native tail columns: 37

* CERN-only types — CONFIRM existing tail as canonical (light review)
| Confirm | Type | Current tail columns |
|---------+------+----------------------|
| yes | regulators | regulator_type |
| yes | optocouplers | optocoupler_type, channels |
| yes | dc_dc_converters | (none — grab-bag) |
| yes | crystals_oscillators | xtal_type, frequency |
| yes | relays | relay_kind, coil_voltage |
| yes | fuses | fuse_kind, current_rating |
| yes | sensors | (none — grab-bag) |
| yes | transformers | (none — grab-bag) |
| yes | power_supplies | (none — grab-bag) |
| yes | thermistors_varistors | device_type, voltage |
| yes | batteries | battery_kind, voltage |

* Deferred: resistors, capacitors (populated passives; fragments defined later as supersets)